                                  main.py
# 用户访问网站
#     ↓
# Uvicorn 启动
#     ↓
# 找到 app = create_app()
#     ↓
# ┌─────────────────────────────────────┐
# │  create_app() 执行流程               │
# ├─────────────────────────────────────┤
# │  1. get_settings()                   │
# │     ↓                                │
# │     读取 .env 配置文件                │
# │     ↓                                │
# │     返回 settings 对象               │
# ├─────────────────────────────────────┤
# │  2. FastAPI(...)                     │
# │     ↓                                │
# │     创建应用实例                      │
# │     ↓                                │
# │     执行 lifespan() 的 yield 前部分  │
# │     （启动准备工作）                  │
# ├─────────────────────────────────────┤
# │  3. include_router()                 │
# │     ↓                                │
# │     挂载所有路由                      │
# ├─────────────────────────────────────┤
# │  4. 返回 application 对象             │
# └─────────────────────────────────────┘
#     ↓
# 网站开始运行，可以接收请求
#     ↓
# 用户关闭网站
#     ↓
# 执行 lifespan() 的 yield 后部分
# （关闭清理工作）

# 第二部分：定义应用生命周期管理
# @asynccontextmanager	装饰器：把下面的函数变成"异步上下文管理器"。
# 比喻：给这个函数贴上"自动开关门"的标签，进门前自动开灯，出门后自动关灯
# async def lifespan(...)	异步函数：可以处理需要等待的操作
# _: FastAPI	参数名用 _ 表示"我不需要这个参数"，但 FastAPI 会传入应用对象
# -> AsyncIterator[None]	类型注解：说明这个函数返回一个异步迭代器（暂停后继续）
# 管理应用的生命周期，yield 之前：应用启动时执行（连接数据库等），yield 之后：应用关闭时执行（断开连接等）
# yield	暂停点：把程序"暂停"在这里，分成前后两部分,

# 第三部分：定义应用工厂函数，使用工厂模式，方便在不同环境（测试/生产）复用，返回一个FastAPI 应用实例
# 网站名称 调试模式  应用版本号  生命周期管理函数

# 3. 挂载路由（把各个功能页面添加到应用中）
# 总路由 统一前缀：/api/v1

# 第四部分：创建全局 app 对象（Uvicorn 会用到）


                               routes/router.py
#                     FastAPI 应用
#                          │
#                 ┌────────┴────────┐
#                 │    include_router│
#                 │   prefix=/api/v1 │
#                 └────────┬────────┘
#                          │
#                     api_router
#               (总路由器/总收纳盒)
#                          │
#          ┌───────────────┼───────────────┐
#          │               │               │
#    include_router  include_router  include_router
#    tags=["system"] tags=["user"]  tags=["chat"]
#          │               │               │
#    health.router    user.router      chat.router
#   (健康检查路由)    (用户路由)       (聊天路由)
#          │               │               │
#    ┌─────┴─────┐    ┌────┴────┐    ┌────┴────┐
#    │  /health   │    │ /login   │    │ /chat   │
#    │ (GET)      │    │ (POST)   │    │ (POST)  │
#    └─────────────┘    └──────────┘    └─────────┘
#
# 最终访问地址：
# - GET  /api/v1/health   ← health 路由
# - POST /api/v1/login    ← user 路由
# - POST /api/v1/chat     ← chat 路由

# api_router	总路由。
# .include_router()	添加子路由的方法
# health.router	子路由器——健康。
# tags=["system"]	给这个子路由打上"系统"标签。部门标签


                                        schemas/health.py
# ┌─────────────────────────────────────────┐
# │  GET /api/v1/health                     │
# ├─────────────────────────────────────────┤
# │  Response 200                           │
# │  ┌───────────────────────────────────┐  │
# │  │ HealthResponse Model              │  │
# │  │ {                                │  │
# │  │   "status": "ok",        <─ 固定值 │  │
# │  │   "app_name": "string",  <─ 字符串 │  │
# │  │   "environment": "string" <─ 字符串│  │
# │  │ }                                │  │
# │  └───────────────────────────────────┘  │
# └─────────────────────────────────────────┘

# 从 Python 的 typing 模块导入 Literal 类型。用来限制某个字段只能取特定的值，不能是其他值
# 导入 Pydantic 的 BaseModel 基类。所有数据模型都必须继承它，才能获得 Pydantic 的强大功能：
# 数据验证：检查数据是否符合类型要求。 类型转换：自动把字符串 “123” 转成整数 123
# 自动文档：FastAPI 会自动生成接口文档。 序列化：自动把对象转换成 JSON


                                    config.py
# 导入必要的工具库
#     ↓
# 找到项目根目录（为了定位 .env 文件）
#     ↓
# 定义 Settings 类（配置数据模型）
#     ↓
# 自动从环境变量和 .env 文件读取配置
#     ↓
# 用 lru_cache 缓存配置对象（避免重复读取）

# 导入 lru_cache 装饰器，缓存函数的结果，避免重复计算
# 导入 Path 类，用来处理文件路径。方便地进行路径操作（拼接、向上找目录等）
# 限制某个字段只能取特定的几个值
# 限制某个字段只能取特定的几个值

# 找到项目根目录
# __file__	当前文件的路径（config.py 的位置）
# .resolve()	把相对路径转成绝对路径
# .parents[3]	向上找 3 级父目录

# 继承自 BaseSettings，有自动读取环境变量、类型转换的能力
# 字段取值优先级：系统环境变量 > .env > 默认值
# 运行环境：只能是 "development"、"test" 或 "production" 之一。默认值："development"（开发环境）

# SettingsConfigDict告诉get_settings()怎么读.env文件
# env_file	指定从哪个 .env 文件读取配置
# env_file_encoding	.env 文件的编码格式
# extra="ignore"	如果 .env 文件里有未声明的配置项，忽略它

# 第一次调用settings1 = get_settings()，会返回Settings类的实例化，读取了 .env 文件
# 第二次调用settings2 = get_settings()，不会读取 .env 文件，直接返回缓存的结果
# 避免重复读取 .env 文件


# 应用启动
#     ↓
# 调用 get_settings()
#     ↓
# ┌─────────────────────────────────────┐
# │  检查缓存中是否有配置对象             │
# ├─────────────────────────────────────┤
# │  是 → 直接返回缓存（不读取 .env）    │
# │  否 → 读取 .env 文件                │
# │       ↓                             │
# │       读取环境变量                   │
# │       ↓                             │
# │       创建 Settings 对象            │
# │       ↓                             │
# │       存入缓存                      │
# │       ↓                             │
# │       返回配置对象                  │
# └─────────────────────────────────────┘


                                        routes/heath.py
# 用户访问 /api/v1/health
#         ↓
# Uvicorn 收到请求
#         ↓
# FastAPI 的总路由api_router先找到对应的子路由router
#         ↓
# 调用 health_check() 函数
#         ↓
# ┌─────────────────────────────────┐
# │  health_check() 执行流程        │
# ├─────────────────────────────────┤
# │  1. 获取配置对象                │
# │     ↓                           │
# │     settings = get_settings()   │
# ├─────────────────────────────────┤
# │  2. 创建 HealthResponse 对象    │
# │     ↓                           │
# │     填充三个字段：              │
# │     - status: "ok"              │
# │     - app_name: 从配置读取      │
# │     - environment: 从配置读取   │
# ├─────────────────────────────────┤
# │  3. 返回 HealthResponse         │
# └─────────────────────────────────┘
#         ↓
# FastAPI 自动验证返回数据
#         ↓
# 转换为 JSON 返回给用户

# Settings：配置类的类型定义（就像"配置清单的模板"）get_settings：获取配置对象的函数（就像"拿出配置清单"，拿公司信息的复印件）
# 导入健康检查的响应数据结构。预先设计好的"健康报告模板"，规定好报告里必须包含哪些字段。产品的要求

# @router.get	装饰器：把下面的函数注册成 GET 接口
# "/health"	路径：用户访问这个地址时触发
# response_model=HealthResponse	响应模型：规定返回数据的结构
# summary="..."	文档摘要：在文档中显示的说明
# async	异步函数，可以处理并发请求
# health_check 函数：健康检查只检查程序本身，不检查数据库等外部服务。
# () -> HealthResponse	类型标注：说明这个函数返回 HealthResponse 类型的数据

# 把get_settings()里面的配置信息给 给 类型为配置类型Settings的变量——settings
# 返回一个 HealthResponse 对象，包含三个字段。


                                tests.py
# 导入测试工具和我们的网站应用
#     ↓
# 创建一个“模拟浏览器”
#     ↓
# 测试 1：访问健康检查接口，看返回内容对不对
#     ↓
# 测试 2：访问一个不存在的地址，看是否报错 404

# 从 FastAPI 里面导入 TestClient（测试客户端）。这是一个模拟器，它可以模拟浏览器发送请求，但不需要真的启动服务器。
# 导入我们在 main.py 里创建的那个 app 对象（应用实例）
# 创建一个测试客户端对象，专门用来给 app 发送请求。

# assert中文意思是“断言”。这是 Python 的关键字，意思是：“我断言后面的条件必须成立，如果成立就继续，如果不成立就报错！”
# 不只检查状态码，还检查完整 JSON，避免字段被意外删除或改名。

# pytest test_health.py 运行测试函数：
# 自动扫描项目中的所有测试文件（比如 tests/ 目录下所有 test开头.py的文件，函数也要test开头），避免了函数太多要写太多调用的麻烦，
# 详细的错误报告
# 统计通过/失败数量
# 支持并行运行
# 直接python运行，如果assert报错过多会程序崩溃
